# Nexxivo - Nginx (Laravel)
# Substitua DOMINIO pelo domínio (ex: app.seudominio.com.br)
# Substitua ROOT_PATH pelo path até /public (ex: /var/www/nexxivo-app/nexxivo/public)

server {
    listen 80;
    listen [::]:80;
    server_name DOMINIO;
    root ROOT_PATH;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";
    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        fastcgi_hide_header X-Powered-By;
        fastcgi_read_timeout 300;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# Bloco HTTPS (Certbot adiciona/ajusta após: certbot --nginx -d DOMINIO)
# Se quiser pré-configurar manualmente, descomente e use DOMINIO/ROOT_PATH:
#
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name DOMINIO;
#     root ROOT_PATH;
#
#     ssl_certificate /etc/letsencrypt/live/DOMINIO/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/DOMINIO/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     add_header X-Frame-Options "SAMEORIGIN";
#     add_header X-Content-Type-Options "nosniff";
#     index index.php;
#     charset utf-8;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         include fastcgi_params;
#         fastcgi_hide_header X-Powered-By;
#         fastcgi_read_timeout 300;
#     }
#
#     location ~ /\.(?!well-known).* { deny all; }
# }
